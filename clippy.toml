# Clippy 工程规范（docs/PROJECT.md §18.3）
# lint 级别在 crate Cargo.toml 的 [lints.clippy] 声明；CI 通过 -D warnings 强制。

# 复杂度过高阈值
cognitive-complexity-threshold = 15
# 允许的最大嵌套层级
type-complexity-threshold = 250
# 强制 MSRV 感知的 lint（与 workspace rust-version 一致）
msrv = "1.85"
