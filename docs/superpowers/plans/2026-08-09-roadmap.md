# Media Library & State Consistency Roadmap（持久化任务计划）

> **用途**：本文件是 HMP 后续开发的持久化计划。任何会话（含压缩上下文后）以此文件为唯一权威路线图。执行时按里程碑拆分为独立 detailed plan（仿照 `2026-08-09-playback-transaction.md` 的结构：Task/Interfaces/TDD Steps/无占位符）。
>
> **基线锚点**：`origin/main` HEAD = `d2201a0`（A ✅48da75a / B ✅effeb65 / C ✅dcfaf9f / D ✅032ccf4 / E ✅cd4de0b / E2 ✅307e50a / F ✅d2201a0）。下一优先为里程碑 G（播放器体验收尾）。
>
> **外部审计来源**：main@1cfc622 的第三方审计（12 P1 + MPRIS 规范项 + 媒体库缺口）。批 1（eb9f85f）已修 7 项独立项；本文件覆盖审计第 1-6 步中尚未落地的部分。

## 当前架构地图（代码事实锚点）

| 区域 | 文件 | 关键事实 |
|---|---|---|
| 协议 | `crates/hmp-core/src/ipc.rs` | `DaemonState { playback, queue: QueueSnapshot(完整 Vec<TrackId>), caps, seq, last_error, phase }`；`MAX_FRAME = 1<<20`；`PlayRequest::Playlist` 隐含 QQ |
| 状态 | `crates/hmp-core/src/player.rs` | `PlaybackState { status, current: Option<Track>, position, duration, volume, loop_mode, shuffle, can_seek, buffering, actual_quality, gen }`（gen 为 playback-transaction 计划新增） |
| 队列 | `crates/hmp-core/src/queue.rs` | `QueueCore`（replace/append/insert_after_current/remove/clear/skip_next/advance_on_eos/prev_track/save_state/restore_state）；`QueueSnapshot { tracks: Vec<TrackId>, current, loop_mode, shuffle }` |
| 引擎 | `crates/hmp-daemon/src/engine.rs`（2088 行） | 命令循环 + `publish()` 每次 `queue.snapshot()` 全量 clone；`load_and_play`/`wait_current_applied`（5s 超时→`EngineError::Timeout`）/`rollback_load`；`session: Option<PlaybackSession>`；`current_gen` |
| 解析 | `crates/hmp-daemon/src/player.rs` | `PlaybackDriver` trait；`SourceResolver`（resolve_source_ids/resolve_track）；`EngineError`；`QqSourceResolver` |
| 存储 | `crates/hmp-storage/src/db.rs` | schema v2（relations/playlist_ops/playlists/playlist_tracks/play_events/tracks/local_files…）；`record_play_start -> i64`；`record_play_end(event_id, …)`；`reconcile_remove_absent(provider…)`；`close_stale_sessions` |
| 驱动 | `crates/hmp-player-gst/src/core.rs` | `LoadRequest { track, uri, quality, gen }`；drive() 单 select 循环；`PlayerEvent::PlaybackEnded{gen}`/`Error{gen,..}` |
| MPRIS | `crates/hmp-mpris/src/service.rs`、`crates/hmp-daemon/src/mpris.rs` | 已修：length i64、SetPosition 校验、Seek 超尾→Next、CanPlay、Rate setter、MIME、CanQuit+quit 转发 |
| CLI | `crates/hmp-cli/src/` | main.rs/playlist.rs/library.rs/comment.rs/scan.rs；`hmp library tracks` 仅 liked 视图；`hmp scan` 在 cli/src/scan.rs（递归收集 + `is_audio_ext`，upsert-only） |

**已完成（勿重复）**：subscribed 只读权限、MIGRATION_V1/V2 事务、library sync playlists_pending、退避节流、song_id 错误标记、phase=Idle 三处、delete_playlist 级联清 ops、评论 cache cap、--sort 校验、IPC roundtrip 测试、cache_fresh、settled/migration/rollback 测试、启动 reconcile、双向 reconcile（provider 过滤）、Reconcile 先消费 outbox、空 present 全清、relation_desired optional、local→owned 拒绝、wait 超时失败、record_play_end 事务、close_stale_sessions、MPRIS 规范六项、is_audio_ext ape/aiff。

---

## 里程碑 A：Playback transaction（✅ 已完成 48da75a）

**成果**：load generation（PlaybackState.load_gen/LoadRequest.load_gen/PlayerEvent 代际，删除 500ms 窗口）、ACK 后提交 + 失败回滚装载（AppliedLoad/rollback_load）、显式 PlaybackSession（event id + listened_ms 旧位置）。
**遗留限制（已知）**：真实 GstDriver 乐观 ACK——坏 URI 的真装载失败表现为同代 Error（仅发布不回滚）；回滚路径仅由超时模型（FakeDriver）覆盖（engine.rs 注释已说明）。

---

## 里程碑 B：SQLite/outbox 单事务原子化（✅ 已完成 effeb65）

**计划**：`2026-08-09-milestone-b-atomic-outbox.md`（3 Task）。
**成果**：storage 4 个组合方法（add_owned_track_with_op / remove_owned_track_with_op / unfavorite_playlist / mark_pending_with_delete_op）+ server.rs 4 处调用点替换；5 个失败注入测试（DROP TABLE 制造第二步失败）；review 无 blocker。

---

## 里程碑 C：QueueSummary 状态协议（✅ 已完成 dcfaf9f）

**计划**：`2026-08-09-milestone-c-queue-summary.md`（4 Task）。
**成果**：QueueCore.revision/summary；DaemonState.queue → QueueSummary；engine queue_tx/queue_rx 独立 watch（仅结构变化发送）；server Queue/QueueList 改读 queue_rx；10k 队列状态帧体积测试；review 无 blocker（修 set_loop_mode no-op guard）。
**注意**：DaemonState.queue 类型变更为破坏性协议变更（旧客户端反序列化失败）——CLI/daemon 同仓同步发布可接受，发布说明需注明。

---

## 里程碑 D：Session restore（✅ 已完成 032ccf4）

**计划**：`2026-08-09-milestone-d-session-restore.md`（3 Task）。
**成果**：`playback_state.json`（$XDG_DATA_HOME/hmp，tmp+rename 原子写）存 QueueState+volume+position；启动恢复队列/音量（不自动播放），首次 Play 装载同曲时 Seek 续播；publish 脏检查节流持久化（队列结构/音量/播放状态翻转立即写、position ≥5s 节流、Paused/Stopped 立即写）；退出前最终写；写失败仅 warn。review 3 项修复：镜像初始化/playing 写后更新/节流测试真实化。
**决策已定**：恢复后不自动播放（用户 Play 后续播）。

---

## 里程碑 E：Local Library domain（✅ 已完成 cd4de0b；watcher 拆 E2）

**计划**：`2026-08-09-milestone-e-local-library.md`（4 Task，watcher 拆 E2）。
**成果**：schema v3（local_files 文件生命周期列 + tracks 完整元数据列 + track_artists + scan_roots，事务迁移）；扫描生命周期（增量 mtime_ns+size 跳过、missing 标记/复位、指纹移动检测、封面提取到 data_dir/covers）；浏览入口（library tracks/albums/artists 聚合 + 搜索过滤 + album:local 播放源分发）；review 3 项修复（record_scan_file 事务化、NOCASE 专辑分组、add_local_file 写 artists）。
**已知限制**：本地专辑身份按名匹配（重名合并）；artist:local 播放源未做；非 UTF-8 路径显示 lossy。

---

## 里程碑 E2：本地库 watcher（✅ 已完成 307e50a）

**计划**：`2026-08-09-milestone-e2-watcher.md`（3 Task）。
**成果**：notify 6 依赖；扫描共享逻辑下沉 hmp-storage::scan（file_fingerprint/persist_cover）+ mark_missing_by_path/scan_root_for（最长前缀）；watcher worker（handler 仅收集、1s 批处理、Create/Modify 自动入库+封面、Remove 标 missing、每轮无条件刷新新增 root、外接盘离线 warn 跳过）；Daemon 保活。review 5 项修复（无条件刷新/短锁/fp 失败跳过/Drop abort/最长前缀）。
**语义**：watcher 不推进 generation、不批量标 missing（复制大目录不误标）；缺失标记由手动全量扫描负责。

---

## 里程碑 F：LibraryPlaylist 播放源（✅ 已完成 d2201a0）

**计划**：`2026-08-09-milestone-f-library-playlist.md`（3 Task）。
**成果**：`PlayRequest::LibraryPlaylist(i64)` 变体（显式拆分，`playlist:<id>` QQ 语义不变）；CLI `playlist:local:<id>` 语法（parse_source 前缀优先）；LocalSourceResolver 解析（playlist_tracks JOIN tracks 按 position,rowid 排序，混排 QQ/本地曲目——source_key 原样 → resolve_track 按 provider 分发既有机制）；Composite 分发；requires_credential 免凭证；daemon_cli 真机集成测试（建歌单→加 wav→播放→status，隔离 XDG 目录）。review 2 项修复。
**已知限制**：add_playlist_track 幂等去重保持（重复曲目/occurrence id 留后续）。

---

## 里程碑 G：播放器体验收尾（审计第 6 步）

**目标**：Previous 曲首行为、输出设备选择、gapless/preload、ReplayGain。
**现状**：MPRIS 规范项已修（批 1）；`Previous` 无"播放 >3s 先回曲首"行为（engine.rs `navigate_prev` 直接上一首）。
**方案**（按序）：
1. Previous 曲首语义：`navigate_prev` 若当前 position > 3s → 仅 seek(0) 不换曲。
2. 输出设备选择：`PlayerCore::new_with_sink` 已有注入点；CLI/配置暴露（`config.toml [audio] sink`）。
3. gapless/preload：`load_and_play` 前预解析下一首 uri（SourceResolver 加 preload 方法）——**依赖 A 的 generation 稳定**。
4. ReplayGain：lofty 读 RG 标签 → volume 补偿（E 的元数据扩展后做）。
**风险**：低-中；依赖：1 独立；2 独立；3 依赖 A；4 依赖 E。

---

## 执行约定

- **顺序**：A（进行中）→ B/C/D（可并行立项，推荐 B→C→D）→ E（大，独立计划）→ F（依赖 E）→ G。
- **每个里程碑**：先写 detailed plan（本目录 `YYYY-MM-DD-<name>.md`，仿 playback-transaction 模板）→ subagent-driven 执行 → review loop → 提交推送 → 更新本文件基线。
- **决策点标注**：本文件中所有"**决策点**"需在写 detailed plan 前由父会话（或 brainstorming）定案。
- **提交**：逐 Task commit；review loop（fresh-context reviewer ×2-3）后统一推送。
- **验证底线**：`cargo test --workspace` + clippy `-D warnings` + fmt + e2e + daemon_cli（--ignored）。
